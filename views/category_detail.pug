extends layout 

block content
    h1= category.name
    ul
        each product in products 
            li
                div
                    a(href=product.url)= product.name
                    img(src=product.img_url)
                    p= product.price + '$'
        else 
            li No products found