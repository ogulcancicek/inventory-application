extends layout 

block content
    h1= title
    ul
        each category in category_list
            li
                a(href=category.url)= category.name

    div(style='margin-top:20px;margin-left:20px').option-menu
        p(style='margin-top:20px;')
            a(href='/') -> Go back main page